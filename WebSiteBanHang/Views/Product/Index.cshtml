@model IEnumerable<WebSiteBanHang.Models.Product>

<h2>Products</h2>

<p>

    <a asp-action="Add" type="button" class="btn btn-primary">Thêm Sản Phẩm</a>
</p>

@if (Model != null && Model.Any()) {
    // Access Model properties inside this block
    <table class="table">
        <thead>
            <tr>
    <th>Name</th>
                <th>Price</th>
                <th>Description</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var product in Model) {
                    <tr>
                        <td>@product.Name</td>
                        <td>@product.Price</td>
                        <td>@product.Description</td>
                    <td>@product.Category?.Name</td>
                        <td>
                        <a asp-action="Display" asp-route-id="@product.Id" type="button" class="btn btn-success">View</a> |
                        <a asp-action="Update" asp-route-id="@product.Id"type="button" class="btn btn-warning">Edit</a> |
                        <a asp-action="Delete" asp-route-id="@product.Id"type="button" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
            }
        </tbody>
    </table>

    @*<div class="container text-center">
        <div class="row">
            @foreach (var product in Model) {
                <div class="col-4>
            <div class="card" style="width: 17rem;">
                        <img src="@product.ImageUrl" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title">@product.Name</h5>
                            <p class="card-text">@product.Description</p>
                            <a asp-action="Display" asp-route-id="@product.Id" type="button" class="btn btn-success">View</a> |
                        <a asp-action="Update" asp-route-id="@product.Id"type="button" class="btn btn-warning">Edit</a> |
                        <a asp-action="Delete" asp-route-id="@product.Id"type="button" class="btn btn-danger">Delete</a>
                            
                        </div>
                    </div>

            }
            </div>

        </div>*@
        


} else {
        <p>No products found.</p>
}

